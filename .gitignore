# environments
venv/
.venv/
env_stk/

# python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/

# generated output -- reproducible from configs + seeds, never committed.
# anchored to the repo root so configs/data/ is not caught by the same pattern
/data/
/previews/
/runs/

# regenerated artefacts: the development-log presentation embeds PNGs, and
# sweep runs write whole datasets, run directories and plots
/presentations/
/sweeps/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
